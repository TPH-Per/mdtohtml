{
  "name": "llm-html-kit-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:watch": "vitest --watch",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tmp-promise": "^3.0.3",
    "turbo": "^2.3.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
